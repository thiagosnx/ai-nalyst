# GitHub Models (primário)
GITHUB_TOKEN=ghp_seu_token_aqui
MODEL_NAME=gpt-4o

# Google Gemini (fallback — usado automaticamente quando o GitHub Models atingir o rate limit)
GOOGLE_API_KEY=
GOOGLE_FALLBACK_MODEL=gemini-2.5-flash

# Paths
DB_PATH=database/bi_factory.duckdb
DBT_PROFILES_DIR=./dbt_layer
DATA_RAW_PATH=data/raw
DBT_MANIFEST_PATH=dbt_layer/target/manifest.json
